# tobira

Minimal Astro site for the `kankaku` landing page at `www.kankaku.net`.

## Commands

```sh
npm install
npm run dev
npm run build
```

Cloudflare Pages build settings:

- Build command: `npm run build`
- Build output directory: `dist`
